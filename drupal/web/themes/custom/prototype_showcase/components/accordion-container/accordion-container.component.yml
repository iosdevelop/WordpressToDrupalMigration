$schema: https://git.drupalcode.org/project/drupal/-/raw/11.x/core/assets/schemas/v1/metadata.schema.json
name: Accordion Container
description: Accordion group matching the exported Canvas pattern.
props:
  type: object
  properties:
    heading:
      type: string
      title: Heading
      examples: ['How the reconstruction works']
slots:
  accordion_items:
    title: Accordion items
    description: Place accordion items here.
